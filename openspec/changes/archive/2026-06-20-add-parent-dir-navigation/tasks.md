## 1. 注入 .. 条目

- [x] 1.1 在 `loadDirectory` 中，获取文件列表并排序后，在数组头部插入 `{ name: '..', path: parentPath, is_dir: true }` 条目（仅当非根目录时）
- [x] 1.2 在缓存命中路径中，同样注入 `..` 条目，确保 parentPath 始终反映当前导航状态
- [x] 1.3 实现根目录检测逻辑：比较 path 的父路径与自身是否相同

## 2. 模板渲染适配

- [x] 2.1 在 `{#each files}` 渲染中，为 `..` 条目显示 📁 图标和 `..` 名称（已有逻辑自然支持，无需额外改动）
- [x] 2.2 确认 `..` 条目的 `data-index` 为 0，后续条目索引正确

## 3. 键盘导航验证

- [x] 3.1 验证 `gg` 跳转到 index 0 选中 `..` 条目
- [x] 3.2 验证 `j/k` 可在 `..` 和其他条目间正常移动
- [x] 3.3 验证 Enter 在 `..` 条目上触发 `onNavigate(parentPath)` 导航到父目录
- [x] 3.4 验证双击 `..` 条目触发导航到父目录

## 4. 边界情况

- [x] 4.1 根目录（如 `C:\`）下不显示 `..` 条目
- [x] 4.2 导航到空目录时 `..` 条目仍然正常显示
- [x] 4.3 从 `..` 导航后，新目录的 `..` 指向正确的父目录
