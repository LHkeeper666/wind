TODO

- 具体支持的功能
- 回收站
- 文件操作异步
- git 状态显示
- 批量重命名
- 任务队列 + 进度（大文件操作，体验保障）
- 插件系统（主题之类）
- 使用搜索打开一个文件后，current应不应该同步更新？
- 选中 exe 类型的文件后，预览界面没有更新
- vim 编辑 normal 模式时，使用中文 j / k 无法移动，而且回车后会进行输入（这个比较重要）

- tab切换时，终端会关闭
- tab切换时，切到一个已经打开终端的tab，状态栏显示 current，但是 j / k 无效果
- 终端打开时，若焦点在终端面板，无法进行 tab 相关操作
- 切换tab后，预览界面没有更新

- 快捷键一览（考虑用一个命令显示所有的快捷键，和）

- 增加一个预览文件树的功能（支持展开合起）
- 查看文件大小（还是说用命令？）

- 全屏编辑还是需要更新一下，重新设计一下，更符合实际使用场景

- 增加一个快捷键用于重置焦点，用于焦点丢失的情况

DONE

- 增加按键提示（实时显示）
- 终端bash显示的问题（done）
- 终端缩放（done）
- 设计一下界面，应该长什么样（done）
- 多标签页（done）
- tab 的时候自动补 \ （done）
- 进入目录时开启预览（done）
- 预览界面，文件大小显示不全（done）
- 回退到上一级时，应该自动定位到之前的位置（done）
- md预览界面上下滑动（done）
- 面板全屏（done）